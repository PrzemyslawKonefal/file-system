import styled from "styled-components";
import styles from "../../config/styles";

const NoContentMessage = styled.p`
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.3em;
  height: 70vh;
  color: ${styles.colors.grey40};
`;

export { NoContentMessage };
